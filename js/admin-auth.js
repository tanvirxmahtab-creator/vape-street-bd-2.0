import { isFirebaseConfigured, auth, signInWithEmailAndPassword, onAuthStateChanged, signOut } from "./firebase-init.js";
import { isOwnerConfigured, isOwner } from "./config.js";
const form=document.querySelector("#login-form"), message=document.querySelector("#login-message");
if (!isFirebaseConfigured || !isOwnerConfigured) { message.textContent=!isFirebaseConfigured?"Firebase connection is not configured yet.":"Set the owner email in js/config.js before enabling the product editor."; message.classList.add("is-error"); form.querySelector("button").disabled=true; }
else { onAuthStateChanged(auth,user=>{if(!user)return;if(isOwner(user.email))location.href="dashboard.html";else{signOut(auth);message.textContent="This account is not approved to manage the catalog.";message.classList.add("is-error");}}); form.addEventListener("submit",async event=>{event.preventDefault();const button=form.querySelector("button");button.disabled=true;message.classList.remove("is-error");message.textContent="Signing in...";try{const credential=await signInWithEmailAndPassword(auth,document.querySelector("#email").value,document.querySelector("#password").value);if(!isOwner(credential.user.email)){await signOut(auth);throw new Error("not-owner")}location.href="dashboard.html"}catch{message.textContent="We could not sign you in with an approved owner account.";message.classList.add("is-error");button.disabled=false}}); }
